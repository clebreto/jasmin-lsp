// Type definitions and variables
param int SIZE = 8;

fn add_one(reg u64 x) -> reg u64 {
    reg u64 result;
    result = x + 1;
    return result;
}

fn multiply(reg u64 a, reg u64 b) -> reg u64 {
    reg u64 result;
    result = a * b;
    return result;
}
