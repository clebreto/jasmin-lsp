// Middle file - requires base and uses its symbols
require "base.jinc"

param int MIDDLE_CONSTANT = 100;

fn middle_function(reg u64 y) -> reg u64 {
  reg u64 temp;
  temp = base_function(y);
  temp = temp + MIDDLE_CONSTANT;
  return temp;
}
